# Runtime dependencies for MonoCruise.
# Pin versions here once the project stabilises.
PySide6
psutil
pygame
requests>=2.33.0
urllib3>=2.7.0
packaging  # PEP 440 version comparison in the updater (stable vs preview)

# Game telemetry / SDK bridges: these may need to be installed from git
# rather than PyPI; adjust to match your local setup if `pip install` fails.
truck_telemetry

